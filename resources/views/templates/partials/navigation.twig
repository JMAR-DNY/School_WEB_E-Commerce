<nav class="navbar navbar-inverse navbar-fixed-top">

    <div class="container-fluid">

        <div class="navbar-header">

            <button type="button" class="navbar-toggle" collapsed data-toggle="collapse" data-target="#navbar" 
            aria-expanded="false">

                <!--<span class="sr-only">Toggle navigation</span>-->

                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>

            </button>

        <a class="navbar-brand" href="{{ path_for('home') }}">EZ-EV<span class="glyphicon glyphicon-home" aria-hidden="true"></span></a>
        
        </div>

    <div class="collapse navbar-collapse" id="navbar">
            <ul class="nav navbar-nav">
                <li><a href="{{ path_for('home')}}">Motors</a><li>
                <li><a href="{{ path_for('home')}}">Controllers</a><li>
                <li><a href="{{ path_for('home')}}">Batteries</a><li>
                <li><a href="{{ path_for('home')}}">Chargers</a><li>
            
                <li><a href="#"><input type="text" placeholder="Search" class="form-control input-sm search-box" /></a></li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path_for('contactUs.index') }}"><span class="glyphicon glyphicon-envelope"></span> Contact Us</a></li>

             {% if basket.check %}
                <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user"></span>{{ basket.user.name }}<span class="caret"></span></a>
                <ul class="dropdown-menu">
                <li><a href="#">Orders</a></li>
                <li><a href="#">Change password</a></li>
                <li><a href="{{ path_for('user.getSignOut')}}">Sign out</a></li>
                </ul>
                </li>
             {% else %}
                <li><a href="{{ path_for('user.index')}}"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                <li><a href="{{ path_for('user.getSignIn')}}"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>   
             {% endif %}
             
             {% if basket.itemCount >0 %}
                <li><a href="{{ path_for('cart.index') }}"><span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span> Cart({{ basket.itemCount }})</a></li>
             {% endif %}
             
            </ul>

        </div>

    </div>

</nav>
